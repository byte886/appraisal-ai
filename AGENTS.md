# AGENTS.md · 本项目给 AI 的操作手册（指令性）

> 给 AI 协作者看：在本项目内干活必须遵守。给人看的介绍见 `README.md`。
> 全局规则在 `~/Doubao/AGENTS.md`，项目管理方法论用 `project-manager` 技能；冲突时本文件在本项目内优先。

## 1. 项目定位与边界
- 这是"房地产评估 AI 产品"项目：逆向竞品（深圳房讯通「灵顾」）+ 自研面向中小评估机构的轻量 AI 出报告产品，首战=个贷抵押报告，试验场在海口一家小型评估公司。
- 当前处于阶段一（逆向与定义）：产出 BRD + 12 份 PRD；**不要越过需求讨论直接进入开发或擅自扩大范围**。
- 我们是竞品付费培训的调研方与潜在竞争对手：资料只用于内部研究与自研，不外传、不公开竞品付费材料原件。

## 2. 目录约定（六槽位，物理映射见 00 总纲第 7 节）
- `00_项目总纲.md`：入口/治理一页纸。`01_结论与产出/`：BRD、PRD 等结论。`02_决策记录/`：ADR。
- `03_进行中的任务/`：TASK_STATUS、ISSUES。`09_调研底稿与素材/`：底稿映射；真实素材在项目根 `竞品情报库/`、`外部渠道素材/`、`training_shots/`。
- 新素材一律先进《素材收件箱_持续登记.md》编号 `Rxx`（已到 R11，新批次从 R12 续），再处理。
- 路径用相对路径或 `~`/`$HOME`，**禁止硬编码 `/Users/<用户名>`**（仓库要跨机/对外）。

## 3. 证据与诚实（硬要求）
- 每条关键结论标证据等级：【事实】/【一方称】/【待核实】/【推断】。竞品报价、效果、客户案例默认"一方称"，未独立核实不得当事实。
- 数字/日期/价格必须来自输入、来源或可复现计算；没有就标"待核实/假设"，绝不编造来源、链接、数值。
- 同事 WorkBuddy 方案约 60% 是 AI"推演"，引用时标注"待估价师校验"，不直接当真实业务定论。
- 工具失败、取不到、读不清，如实说，不用旧快照或臆测填充。

## 4. 禁止事项
- 不改写、不删除、不移动 `竞品情报库/`、`外部渠道素材/`、`training_shots/` 里的**原始素材**（只读；提炼写到新文件）。
- 不把明文密钥、token、主口令写进任何文件或 git（凭证走全局 `secrets` 命令，不落本仓）。
- 不在仓库中新增未经脱敏的个人信息（手机号、银行账号、身份证、私人姓名全称）；确需引用商务条款时角色化/部分打码。
- 不擅自把本仓设为公开、不擅自 push 到第三方、不替用户做对外报价/承诺/签约。
- 不把 `draft_*_folder/`（飞书本地中间稿）、`.DS_Store`、临时切段 `_seg` 之外的大体积缓存提交入库（以 .gitignore 为准）。

## 5. 新会话恢复顺序
冷启动：`README.md` → `00_项目总纲.md` → `01_结论与产出/BRD...md` → `03_进行中的任务/TASK_STATUS.md` + `ISSUES.md` → 按需沿 `09` 映射读底稿。
续接（"继续做 X"）：直接读 TASK_STATUS/ISSUES + 对应上一单元样板成品。

## 6. 提交前检查
- TASK_STATUS/ISSUES 已更新；结论可溯源、证据等级已标；无明文凭证/未脱敏个人信息（可用 `audit-secrets.sh` 扫）。
- 先子模块后父仓；涉及双机同步时对端只 ff-only、禁 pull 合并提交（按 dual-machine-manager）。
- 对外公开范围变更（私有↔公有）必须先经用户确认。
